Fallback folder for resume text.

If a Google Docs link can't be downloaded (private doc, bad link), the bot
looks here for a .txt file named like:

    <Company>_<Role>.txt

Example:  ExampleCorp_Data_Scientist.txt

Just paste the tailored resume text into the file and re-run. The primary
path is always the Google Docs export link in applications.xlsx.
