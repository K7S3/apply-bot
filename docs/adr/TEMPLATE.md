# ADR NNNN: <short title>

- Status: proposed
- Date: YYYY-MM-DD
- Supersedes: (leave blank, or "NNNN: <title>" if this replaces an older ADR)
- Superseded by: (leave blank; filled in if a later ADR replaces this one)

## Context

What problem are we solving, and what forces constrain the answer? Include
the background a new contributor would need: the current state of the code,
the user need, and the tradeoffs on the table. Link to relevant issues,
PRs, or existing ADRs.

## Decision

What we decided, stated plainly. Name the chosen option and, briefly, the
serious alternatives we rejected and why. Reference the modules, commands,
or files affected (e.g. `candid/config.py`, `python -m candid track add`).

## Consequences

- Positive: what gets better because of this decision.
- Negative: what gets worse, and how we mitigate it.
- Neutral: things that change but are neither good nor bad (migration
  notes, follow-up work, things to revisit later).

When a decision is later reversed, do not delete this file: change its
status to `superseded by MMMM` and link the replacement.
