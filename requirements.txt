# candid dependencies
#
# candid is stdlib-only by design. The single optional dependency below is
# needed only for `onboard --resume file.pdf` (PDF text extraction).
# Everything else — matching, tailoring, tracker, dashboard, judge —
# runs on the standard library alone.

pypdf>=3.0.0  # optional: PDF resume parsing
pyyaml>=6.0  # optional: config.yaml read/write (migrate, doctor)
